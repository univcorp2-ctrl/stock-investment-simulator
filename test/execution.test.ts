import{describe,expect,it}from'vitest';import{buildOrderPreview}from'../src/shared/execution';
describe('buildOrderPreview',()=>{it('builds Alpaca trailing stop payload',()=>{const p=buildOrderPreview({brokerId:'alpaca',symbol:'aapl',assetClass:'equity',side:'sell',quantity:3,orderStyle:'trailing_stop',trailPercent:4,timeInForce:'gtc',live:false});expect(p.valid).toBe(true);expect(p.payload).toMatchObject({type:'trailing_stop',trail_percent:'4',symbol:'AAPL'});});it('warns for emulated trailing',()=>{const p=buildOrderPreview({brokerId:'tradier',symbol:'MSFT',assetClass:'equity',side:'sell',quantity:1,orderStyle:'trailing_stop',trailPercent:3,timeInForce:'gtc',live:true});expect(p.valid).toBe(true);expect(p.safetyMode).toBe('blocked-live');expect(p.warnings.join(' ')).toContain('trailing support');});it('rejects missing trailing params',()=>{const p=buildOrderPreview({brokerId:'binance',symbol:'BTCUSDT',assetClass:'crypto',side:'sell',quantity:.1,orderStyle:'trailing_stop',timeInForce:'gtc',live:false});expect(p.valid).toBe(false);expect(p.errors.join(' ')).toContain('trailPercent or trailAmount');});});
